name = "malha"
compatibility_date = "2025-08-01"
pages_build_output_dir = "dist"

# Variáveis de ambiente de BUILD (lidas pelo vite.config.ts via loadEnv)
[vars]
# Nenhuma — variáveis sensíveis vão em [vars] do Worker, não aqui.

# Configuração do projeto Pages
# Deploy: wrangler pages deploy dist
# Domínio: mlh.bobsimcom.br (configurar no painel Cloudflare -> Pages -> Custom domains)
